import React from 'react';
import { BrowserRouter } from './services/providers/router-provider/routerProvider';

const App: React.FC = () => {
    return (
        <BrowserRouter />
    );
};

export default App;